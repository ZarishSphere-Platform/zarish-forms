name: Generate Previews
on:
  pull_request:
    paths:
      - "schemas/**"
      - "spreadsheets/**"

jobs:
  build-previews:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Preview generation step
        run: |
          echo "TODO: integrate Go CLI to generate previews"
